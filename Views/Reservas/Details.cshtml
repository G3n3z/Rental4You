@model Rental4You.Models.Reserva

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Reserva</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Concluido)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Concluido)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Veiculo)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Veiculo.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.ReservaId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>

@if (true)
{
    <form asp-action="Create" asp-controller="Avaliacoes">
        <input type="hidden" name="ReservaId" value="@Model.ReservaId" />
        <input type="hidden" name="EmpresaId" value="@Model.Veiculo.EmpresaId" />
        
        @if(Model.Avaliacao == null)
        {
            <div>
                <label asp-for="Avaliacao.Nota"></label>
                <input name="Nota" value="@Model?.Avaliacao?.Nota" />
            </div>
        <button type="submit" class="btn btn-primary">Submeter Avaliação</button>
        }
        else
        {
            <div>
                <label asp-for="Avaliacao.Nota"></label>
                <input name="Nota" value="@Model?.Avaliacao?.Nota" readonly="readonly"/>
            </div>
        }

            @*<span class="fa fa-star"></span>
                <span class="fa fa-star"></span>
                <span class="fa fa-star"></span>
                <span class="fa fa-star"></span>
                <span class="fa fa-star"></span>*@
    </form>
}
else
{
    @if(Model.Avaliacao != null) { 
        <div>
            <label asp-for= "Avaliacao.Nota" ></label >
            <input asp-for= "Avaliacao.Nota" readonly="readonly"/>
        </div >      }
    else
    {
        <div>Esta reserva não possui Avaliação</div>
    }
}


<script>document.querySelectorAll('.fa').forEach(element => {
        element.addEventListener('onMouseOver', function (event) {

        })
    })</script>
